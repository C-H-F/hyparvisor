{
  "name": "hyparvisor",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "clean": "rimraf _dist",
    "build": "npm run build --workspaces",
    "deploy": "npm run clean && npm run build && npm run join && ncp ./backend/_dist ./_distr && ncp ./frontend/_dist ./_distr/_htdocs"
  },
  "author": "C-H-F",
  "license": "AGPL-3.0",
  "devDependencies": {
    "ncp": "^2.0.0",
    "rimraf": "^5.0.5"
  }
}
